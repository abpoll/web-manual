[project]
authors = [{name = "Adam Pollack", email = "adampollack25@gmail.com"}]
dependencies = []
name = "web-manual"
requires-python = ">= 3.11"
version = "0.1.0"

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["osx-64"]

[tool.pixi.tasks]

[tool.pixi.dependencies]
python = "3.11.*"
pandas = ">=3.0.5,<4"
jupyter = ">=1.1.1,<2"
itables = ">=2.9.1,<3"
pyyaml = ">=6.0.3,<7"
